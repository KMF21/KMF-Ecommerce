// import { apiVersion } from '@/sanity/env'

// if (!process.env.PAYSTACK_SECRET_KEY) {
//   throw new Error('STRIPE_SECRET_KEY IS NOT SET')
// }

// const stripe = new Stripe(process.env.PAYSTACK_SECRET_KEY, { apiVersion: '' })

// export default stripe
